{
  "name": "bitcentive",
  "description": "Royalties",
  "version": "0.0.0",
  "homepage": "https://github.com/donejs/bitcentive#readme",
  "main": "src/",
  "keywords": [
    "feathers"
  ],
  "license": "MIT",
  "repository": {},
  "author": {},
  "contributors": [],
  "bugs": {},
  "engines": {
    "node": "6.11.0"
  },
  "scripts": {
    "test": "npm run jshint && TESTING=true npm run mocha && cd public && npm run test",
    "jshint": "jshint src/. test/. --config",
    "develop": "node src/ --develop",
    "start": "node src/",
    "mocha": "mocha test/ --recursive",
    "install-old": "node db-connect.js && npm run db-migrate",
    "db-migrate": "sleep 1 && node migrate.js"
  },
  "dependencies": {
    "db-migrate": "^0.10.0-beta.20",
    "db-migrate-mongodb": "^1.1.4",
    "mongodb": "^2.0.0",
    "mongoose": "^4.7.0"
  },
  "system": {
    "npmAlgorithm": "flat"
  },
  "devDependencies": {
    "jshint": "^2.9.4",
    "mocha": "^3.2.0",
    "request": "^2.79.0"
  }
}
