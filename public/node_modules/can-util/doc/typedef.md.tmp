@typedef {Object} can-util/typedefs/ajaxOptions ajaxOptions
@parent can-define/typedefs

Defines configuration options for the AJAX request.

@type {Object}

This object is used for AJAX request settings.

```
var ajax = require("can-util/dom/ajax");

ajax({
	method: "GET",
	url: "http://query.yahooapis.com/v1/public/yql",
	data: {
		q: 'select * from geo.places where text="sunnyvale, ca"',
		format: "json"
	}
}).then( successCallback );
```

	@option {String} url The requested url.
	
	@option {String} [type] The method of the request. Ex: `GET`, `PUT`, `POST`, etc. Capitalization is ignored. Default is `GET`.
	
	@option {Object} [data] The data of the request. If data needs to be urlencoded (e.g. for GET requests or for CORS) it is serialized with [can-util/js/param].
	
	@option {String} dataType Type of data. Default is `json`.
	
	@option {String} lang Language. Default is `en`.
	
	@option {String} userAgent Default is `XMLHttpRequest`.

